[default]
model = codex-latest

[g]
prompt = identify spelling or grammar mistakes
options.temperature = 0.4
options.model = gpt-3.5-turbo

[g.edit]
prompt = rewrite with spelling and grammar mistakes removed

[f]
prompt = Generate function signatures for the following functions

[t]
prompt = Generate test cases for the following function signatures

[h]
prompt = Fix this tsx text with approprirate html encodings
